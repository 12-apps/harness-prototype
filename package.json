{
  "name": "proto-harness",
  "version": "3.7.0",
  "description": "Prototyping bench: the prototype is the specification, and the gate enforces it.",
  "bin": { "proto-verify": "./verify.js" },
  "scripts": {
    "verify": "node verify.js proto.html",
    "verify:examples": "node verify.js examples/product-editor.html",
    "catalog": "node scripts/generate-catalog.js"
  },
  "devDependencies": { "jsdom": "^24.0.0" },
  "private": true
}
